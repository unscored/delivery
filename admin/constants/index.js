export const ROUTES_MAP = {
  main: '/',
  login: '/login',
  clients: '/clients',
  orders: '/orders',
};

export const BASE_URL = $__WebPackConfig.API_URL;

export const API = {
  orders: 'orders',
};

export const API_METHODS = {
  getOrders: 'getOrders',
}